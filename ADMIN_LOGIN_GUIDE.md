# 管理员登录界面使用指南

## 概述
管理员登录界面是 CareerAI 系统的后台管理入口，用于管理岗位数据库。只有通过身份验证的管理员才能访问管理后台。

## 访问方式

### 方式一：从学生端导航
1. 打开应用首页
2. 在左侧边栏底部找到 **"管理员登录"** 链接
3. 点击进入登录页面

### 方式二：直接访问
- 本地开发：`http://localhost:5173/admin/login`
- 生产环境：`http://187.124.249.235/admin/login`

## 登录界面说明

### 界面布局
```
┌─────────────────────────────────┐
│         🎓 AI职业规划            │
│        管理员登录                 │
├─────────────────────────────────┤
│  [👤 管理员账号]                 │
│  [🔒 密码]                       │
│  [登录管理后台]                   │
├─────────────────────────────────┤
│  ← 返回学生端                    │
└─────────────────────────────────┘
```

### 界面元素

| 元素 | 说明 |
|------|------|
| 账号输入框 | 输入管理员用户名，必填项 |
| 密码输入框 | 输入管理员密码，支持显示/隐藏密码 |
| 登录按钮 | 点击提交登录请求，登录中显示加载状态 |
| 返回链接 | 返回学生端首页 |

## 登录凭证

| 项目 | 值 |
|------|-----|
| 默认账号 | `admin` |
| 默认密码 | `admin123` |
| 环境变量 | `ADMIN_USER`、`ADMIN_PASS` |

> ⚠️ **安全提示**：生产环境应通过环境变量修改默认凭证，不要使用默认密码。

## 登录流程

### 1. 输入凭证
- 在 **管理员账号** 字段输入用户名
- 在 **密码** 字段输入密码
- 支持按 `Enter` 键快速提交

### 2. 验证
系统将向后端发送 POST 请求：
```
POST /api/admin/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}
```

### 3. 响应处理

**登录成功（HTTP 200）**
```json
{
  "code": 0,
  "token": "career-ai-admin-token-2025",
  "message": "登录成功"
}
```
- Token 保存到 `localStorage` 的 `adminToken` 字段
- 自动跳转到管理后台首页 `/admin`
- 显示成功提示

**登录失败（HTTP 401）**
```json
{
  "code": 401,
  "message": "用户名或密码错误"
}
```
- 显示错误提示
- 保留输入框内容，允许重新尝试

**网络错误**
- 显示 "网络错误，请稍后重试"
- 检查网络连接

## 登录后

### 获得的权限
登录成功后，管理员可以访问以下功能：

| 功能 | 说明 | API 端点 |
|------|------|---------|
| 查看岗位列表 | 分页查看所有岗位数据 | `GET /api/admin/positions` |
| 新增岗位 | 添加新的岗位信息 | `POST /api/admin/positions` |
| 编辑岗位 | 修改现有岗位信息 | `PUT /api/admin/positions/:id` |
| 删除岗位 | 删除岗位记录 | `DELETE /api/admin/positions/:id` |

### Token 机制
- 登录后的 Token 存储在浏览器 `localStorage` 中
- 每次请求管理员 API 时，Token 通过 `Authorization` 请求头发送：
  ```
  Authorization: Bearer career-ai-admin-token-2025
  ```
- Token 在浏览器关闭后仍然保留（持久化存储）
- 需要手动退出登录才能清除 Token

### 退出登录
在管理后台页面，点击左侧边栏底部的 **"退出"** 按钮：
- 清除 `localStorage` 中的 `adminToken`
- 自动跳转回学生端首页

## 常见问题

### Q1: 忘记密码怎么办？
**A:** 联系系统管理员修改 `.env` 文件中的 `ADMIN_PASS` 环境变量，然后重启后端服务。

### Q2: 登录后页面一片空白？
**A:** 
- 检查浏览器控制台是否有错误信息
- 确认后端服务是否正常运行
- 清除浏览器缓存后重试

### Q3: 能否同时多个管理员登录？
**A:** 可以。每个管理员使用相同的账号密码登录，系统不区分具体是哪个管理员。

### Q4: Token 过期了怎么办？
**A:** 当前系统的 Token 不会过期。如需实现 Token 过期机制，需要修改后端代码添加时间戳验证。

### Q5: 如何修改管理员账号密码？
**A:** 修改后端 `.env` 文件：
```env
ADMIN_USER=your_username
ADMIN_PASS=your_password
```
然后重启后端服务。

## 技术细节

### 前端实现
- **框架**：Vue 3 + TypeScript
- **UI 库**：Element Plus
- **表单验证**：Element Plus Form 组件
- **存储**：localStorage

### 后端实现
- **框架**：Express.js
- **认证方式**：Bearer Token
- **验证函数**：`adminAuth` 中间件
- **数据库**：MySQL

### 安全考虑
- ✅ 密码通过 HTTPS 传输（生产环境）
- ✅ Token 存储在 localStorage（仅限同源访问）
- ✅ 所有管理员 API 都需要 Token 验证
- ⚠️ 当前系统使用硬编码 Token，生产环境应改为 JWT
- ⚠️ 建议添加登录失败次数限制和账号锁定机制

## 相关文件

| 文件 | 说明 |
|------|------|
| `src/views/Login.vue` | 登录界面前端代码 |
| `backend/app.js` | 后端登录 API 实现 |
| `.env` | 环境变量配置文件 |

## 更新日志

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | 2025-04-18 | 初始版本 |
